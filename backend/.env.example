# ===========================================
# CollegeSite Backend Environment Configuration
# ===========================================

# Database Configuration
# ----------------------
# For PostgreSQL (Production - Render):
# DATABASE_URL=postgresql://user:password@host:5432/database_name
#
# For SQLite (Local Development):
# Leave DATABASE_URL empty or unset, will use COLLEGE_DB_PATH
COLLEGE_DB_PATH=./college_reports.db

# Server Configuration
# --------------------
HOST=0.0.0.0
PORT=8000

# CORS Configuration
# ------------------
# Comma-separated list of allowed origins
# Development:
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000,http://127.0.0.1:8080
# Production (set to your Vercel frontend URL):
# CORS_ORIGINS=https://your-app.vercel.app

# Security
# --------
# IMPORTANT: Generate a secure random key for production!
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-secret-key-here-change-in-production

# ===========================================
# Deployment Notes
# ===========================================
#
# Vercel (Frontend):
# 1. Set VITE_API_URL to your Render backend URL
#    Example: https://college-reports-api.onrender.com/api
#
# Render (Backend):
# 1. DATABASE_URL is automatically set when you link a PostgreSQL database
# 2. Set CORS_ORIGINS to your Vercel frontend URL
# 3. JWT_SECRET_KEY can be auto-generated via render.yaml
#
# Local Development:
# 1. Copy this file to .env
# 2. Adjust values as needed
# 3. Run: docker-compose up
